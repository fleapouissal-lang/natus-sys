# Natus — loyalty.natusmarrakech.com (bootstrap HTTP, sans SSL)
# Sous-domaine public dédié à l'espace carte fidélité (PWA active).
# Proxy vers la MÊME application Next.js (PM2, port 3002) — le routage par host
# est géré dans le middleware Next.js (le host est transmis via "Host").
#
# Utiliser AVANT certbot, puis certbot ajoute le bloc 443 :
#
#   sudo cp deploy/nginx/loyalty.natusmarrakech.com.bootstrap.conf \
#     /etc/nginx/sites-available/loyalty.natusmarrakech.com
#   sudo ln -sf /etc/nginx/sites-available/loyalty.natusmarrakech.com /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#   sudo certbot --nginx -d loyalty.natusmarrakech.com

server {
    listen 80;
    listen [::]:80;
    server_name loyalty.natusmarrakech.com;

    client_max_body_size 20M;

    access_log /var/log/nginx/loyalty.natusmarrakech.com.access.log;
    error_log /var/log/nginx/loyalty.natusmarrakech.com.error.log;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://127.0.0.1:3002;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_cache_bypass $http_upgrade;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
